import React,{useEffect,useState}from'react';import{createRoot}from'react-dom/client';import'./style.css';function App(){const[items,setItems]=useState([]);async function load(){try{const r=await fetch('http://localhost:5000/api/customers');setItems(await r.json())}catch{setItems([])}}useEffect(()=>{load()},[]);return <main><h1>CRUD_clientes</h1><p>Frontend React integrado ao backend .NET.</p><button onClick={load}>Atualizar</button>{items.map(x=><article key={x.id}>{x.name} • {x.email}</article>)}</main>}createRoot(document.getElementById('root')).render(<App/>);
